<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @product.name %>
</p>

<p>
  <strong>Price:</strong>
  <%= number_to_currency(@product.price) %>
</p>

<p>
  <strong>Image:</strong>
  <%= image_tag(@product.image.url(:medium)) %> 
  (<%= @product.image_id %>)
</p>

<p>
  <strong>Store:</strong>
  <%= @product.store.name %> (<%= @product.store_id %>)

</p>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
