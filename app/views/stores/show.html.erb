<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @store.name %>
</p>

<p>
  <strong>Address:</strong>
  <!-- Doesn't work, you have to loop through it because it's a one to many relationship -->
  <%#= @store.address.name %>
  <% @store.addresses.each do |address| %>
    <%= address.name %>, <%= address.city %>
  <% end %>
  (<%= @store.address_id %>)
  
</p>
<!--
<h2>Products</h2>
<p>
  <%# @store.products.each do |product| %>
    <p><%#= image_tag(product.image(:thumb)) %> <%#= link_to product.name, product %>: <%#= number_to_currency(product.price) %> </p>
  <%# end %>
  
</p>
-->

<div class="pure-g">
  <% @store.products.each do |product| %>
    <div class="photo-box u-1 u-med-1-2 u-lrg-1-3">
      <%#= link_to(product.name) %>
      <%= image_tag(product.image(:xlarge)) %>

    
      <aside class="photo-box-caption">
        <span>
          <%= link_to(product.name, product) %> <%= number_to_currency(product.price) %>
        </span>
      </aside>
    </div>
  <% end %>   
</div>   
            
            
<%= link_to 'Edit', edit_store_path(@store) %> |
<%= link_to 'Back', stores_path %>
